# 安全设计说明

## 通信

服务器仅接受 TLS 1.2 或更高版本连接。桌面用户端、手机端、管理员端和设备模拟器使用 `QSslSocket`，并信任项目内置的课程服务器证书。因此 JSON 帧、密码、订单和余额不会以明文经过网络。

Windows Qt 5.15.2 运行 TLS 时需要匹配架构的 OpenSSL 1.1.x 运行库。若服务器报告 `TLS initialization failed`，请通过 Qt 安装维护工具安装 OpenSSL 运行组件，或把官方匹配的 `libssl-1_1-x64.dll` 与 `libcrypto-1_1-x64.dll` 放到可执行文件目录。OpenSSL 3.x DLL 不能替代它们。

`config/server-cert.pem` 和 `config/server-key.pem` 是仅供课程局域网演示的证书。正式部署时必须更换，并只允许服务器部署人员读取私钥；客户端仅分发公钥证书。

## 密码

- 客户端不保存密码，也不记录密码日志。
- TLS 负责传输机密性和服务器身份校验；没有采用“客户端先哈希再发送”，因为固定哈希会成为可重放的等价密码。
- 服务端为每个账户生成 128 位随机盐，并使用 Qt 自带 `QCryptographicHash` 进行 50000 轮 SHA-256 派生。
- 哈希比较采用常量时间比较。
- 登录和充值共享失败次数；成功验证后清零，累计连续失败 5 次后状态变为 `LOCKED`。
- 管理员用户管理页的“解锁 / 解冻”会恢复 `NORMAL` 并清零失败次数。

## 数据库迁移

服务端启动时会自动为旧 SQLite 数据库增加 `password_salt`、`password_hash`、`failed_attempts` 和 `locked_at` 等字段。旧测试用户没有密码哈希，可以通过注册流程为原手机号补设密码。旧管理员第一次成功登录后自动升级哈希。
